import React, { Component } from 'react';
import { View, Text, ScrollView, StyleSheet } from 'react-native';

class CreateDecks extends Component {
  render() {
    return (
      <ScrollView><Text>CreateDecks</Text></ScrollView>
    )
  }
}

export default CreateDecks;